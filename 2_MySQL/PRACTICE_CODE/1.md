```sql
CREATE DATABASE School;
USE School;


-- CREATE Command
-- Part 1 : Table Name
-- Part 2 : Columns Definition
CREATE TABLE IF NOT EXISTS Students ( 
	id BIGINT AUTO_INCREMENT PRIMARY KEY, 
	fullName VARCHAR(20),
    class INT,
    section CHAR,
    address TEXT,
    gender ENUM("M","F")
);

-- Column_Name 
-- DATA_TYPE 
-- Constraints






-- fullName VARCHAR(20), class INT, section CHAR, address TEXT, gender ENUM("M","F")


SHOW DATABASES;

-- ALTER Command
ALTER TABLE Students MODIFY COLUMN section ENUM("A", "B", "C");
ALTER TABLE Students MODIFY COLUMN id BIGINT;

-- RENAME Command
RENAME TABLE Students TO StudentsTable;

-- DROP Command
DROP TABLE Students;

-- TRUCNCATE Command
TRUNCATE Students;

-- DESC Command
DESC StudentsTable;
DESCRIBE Students;

-- INSERT 
INSERT INTO Students VALUES (1, "Tom", 5, "A", "","M" );
INSERT INTO Students VALUES (2, "Jerry", 7, "B", "","M" );
INSERT INTO Students VALUES (3, "Sara", 8, "A", "","F" );

-- SELECT 
SELECT * FROM Students;

SHOW DATABASES;
```