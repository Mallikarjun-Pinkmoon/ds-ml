```sql
CREATE DATABASE HelloWorld;
USE HelloWorld;

CREATE TABLE IF NOT EXISTS Human (
	id BIGINT PRIMARY KEY AUTO_INCREMENT,
    fullName VARCHAR(30) NOT NULL,
    phoneNumber VARCHAR(10) UNIQUE,
    state ENUM("AP", "TN", "KN") DEFAULT "AP",
    gender ENUM("M","F") NOT NULL,
    dob DATE NOT NULL
);






INSERT INTO Human(fullName, phoneNumber, state, gender, dob) 
VALUES 
	("Tom", "123", "AP", "M", "2020-03-28"), 
    ("Sara", "456", "KN", "F", "2023-07-6"), 
    ("Jerry", "789", "TN", "M", "2006-09-2");

INSeRt INTO Human(fullName, phoneNumber, State, gender, DOB) VALUES ("TOM", "78895", "AP", "M", "2020-01-05");
INSERT INTO Human(fullName, phoneNumber, State, Gender, DOB) VALUES ("JOHN","45866", "KN", "M","2021-05-25");
INSERT INTO Human(fullName, phoneNumber, State, Gender, DOB) VALUES ("JERRY","7808", "TN", "F","2022-04-15");



SELECT fullName, phoneNumber, state, gender, dob
FROM Human
WHERE (state = "KN" AND gender = "M"); 


SELECT * FROM Human;

TRUNCATE Human;

DROP TABLE Human;

DESC Human;
```